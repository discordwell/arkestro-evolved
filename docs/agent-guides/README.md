# Agent Guides

This repository supports four operator surfaces:

- Codex via `AGENTS.md` and `.agents/skills/controlplane`
- Claude Code via `CLAUDE.md`, `.claude/commands`, `.claude/agents`, and `.claude/hooks`
- OpenClaw via `openclaw/skills`
- ChatGPT via `apps/chatgpt-app`

All of them should use the same runbook catalog and public API.

