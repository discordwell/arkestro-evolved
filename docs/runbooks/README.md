# Runbooks

The runbook catalog is defined in [`catalog/runbooks.json`](../../catalog/runbooks.json).

The four v1 runbook families are:

- Release Coordination
- Incident Response
- Support Triage
- Compliance Pack

These docs mirror the shared catalog used by the Go service, CLI, MCP server, and agent-facing files.

